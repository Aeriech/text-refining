# Google GenAI SDK key (Gemini Free Tier)
GEMINI_API_KEY=your_api_key_here

# Address the server binds to
PORT=8080

# Comma-separated allowed CORS origins (Next.js dev server)
CORS_ORIGINS=http://localhost:3000
